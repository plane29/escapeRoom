public interface OrderedCollection{ //ordered collection from class
    public void append(int toAppend);  //takes in an int and appends it to end
    public int peek();  //looks at the end
    public int pop();  //pops off the end and shows it
    public String toString();  //converts list to string
    public int length();  //returns length of ds
}